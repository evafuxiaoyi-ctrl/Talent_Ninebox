#!/usr/bin/env bash
set -euo pipefail

export PYTHONPATH="/code:${PYTHONPATH:-}"
export APP_ACCESS_PASSWORD="${APP_ACCESS_PASSWORD:-change-me}"
export APP_SESSION_SECRET="${APP_SESSION_SECRET:-change-me-session-secret}"

exec python3 -m uvicorn talent_ninebox.web.app:app --host 0.0.0.0 --port 9000
